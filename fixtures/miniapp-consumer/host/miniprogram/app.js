// This host owns the UI. It is WXML, WXSS and thin JavaScript on purpose: the SDK does not render,
// and neither does the consumer's shared Kotlin. Everything below calls the bundle this fixture
// assembled.
App({});
